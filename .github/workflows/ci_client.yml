name: client ci

on:
  pull_request:
    branches: [main]
  push:
    branches: [main]
  paths:
    - "client/**"

jobs:
  styles:
    name: Style
    on: ubuntu-latest

  steps:
    - name: Check out code
      uses: action/checkout@v4
    
    - name: Setup node
      uses: action/setup-node@v4
      with:
        node-version: 22.12.0

    - name: Install dependencies
      run: |
        cd client
        npm install

    - name: Lint
      run: |
        cd client
        npm run lint
